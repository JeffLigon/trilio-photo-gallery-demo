apiVersion: v1
kind: ConfigMap
metadata:
  name: trilio-gallery-sql
  namespace: trilio-demo
data:
  # Keep these filenames exactly; the initContainer mounts them at /db/
  schema.sql: |
    
    CREATE DATABASE IF NOT EXISTS triliogallery;
    USE triliogallery;
    CREATE TABLE IF NOT EXISTS photos (
      id INT AUTO_INCREMENT PRIMARY KEY,
      title VARCHAR(120) NOT NULL,
      caption VARCHAR(240) DEFAULT NULL,
      filename VARCHAR(255) NOT NULL,
      size_bytes BIGINT DEFAULT 0,
      created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );

  seed.sql: |
    USE triliogallery;
    DELETE FROM photos;
    INSERT INTO photos (title, caption, filename, size_bytes) VALUES
      ('Application Recovery','', 'blog-01-application-recovery.jpg', 163267),
      ('Oracle Kubernetes Engine Backup','', 'blog-02-oracle-kubernetes-engine-backup.jpg', 274695),
      ('Granular Recovery Tech','', 'blog-03-granular-recovery-tech.jpg', 253668),
      ('Changed Block Tracking','', 'blog-04-changed-block-tracking.jpg', 188475),
      ('Redis Backup Strategies','', 'blog-05-redis-backup-strategies.jpg', 179432),
      ('Kubernetes Pvc Backup','', 'blog-06-kubernetes-pvc-backup.jpg', 240022),
      ('Proxmox Vs Openstack','', 'blog-07-proxmox-vs-openstack.jpg', 273353),
      ('Snapshot Vs Backup','', 'blog-08-snapshot-vs-backup.jpg', 220570),
      ('Red Hat Openshift On Azure','', 'blog-09-red-hat-openshift-on-azure.jpg', 218506),
      ('Test Backup Recoverability','', 'blog-10-test-backup-recoverability.jpg', 220324),
      ('Kubecon Europe Highlights','', 'blog-11-kubecon-europe-highlights.jpg', 179800),
      ('Openshift Data Foundation','', 'blog-12-openshift-data-foundation.jpg', 237054);